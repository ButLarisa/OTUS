//Задание

function sum(a, b){
    if(b === undefined){
        return (c) => {
            return c + a;
        }
    }
    return a + b;
}



// 2 вариант без проверки
 
 
 let args = [];
 let sum = 0;

 function add(arg) {

    args.push(arg);
    
    sum  = args.reduce( (a, b) => a + b);
    
    console.log('sum = ' + sum);
}



